import { SafeAreaView, Text } from 'react-native'

function MapScreen() {
  return (
    <SafeAreaView>
      <Text style={{ padding: 30 }}>MapScreen</Text>
    </SafeAreaView>
  )
}

export default MapScreen

// const styles = StyleSheet.create({})
